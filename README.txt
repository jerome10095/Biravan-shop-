Drop your hero background video here, named exactly:

  hero-loop.mp4

Recommended specs:
- Format: MP4 (H.264)
- Length: 6-15 seconds, looping seamlessly
- No audio needed (it plays muted)
- Resolution: 1920x1080 or larger, landscape
- Keep the file under ~8-10MB for fast loading (compress with HandBrake or similar if needed)

Once the file is in place, the hero section on the homepage will play it automatically.
Until then, the fallback photo behind it is shown instead.
